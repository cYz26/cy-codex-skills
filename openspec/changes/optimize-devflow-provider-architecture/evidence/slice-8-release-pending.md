# Slice 8 Release and Local Reference Evidence

The filename is retained for compatibility with the approved task ledger; the
release portion is complete. Installed-cache refresh and real-project migration
remain separate, unapplied actions.

## Source and Packaged Verification

- Complete development suite: `Ran 399 tests in 59.332s`, `OK`.
- Focused wrapper/template repair suite: 68 tests, `OK`; its RED run first
  reproduced both the bytecode side effect and the missing exact ledger fields.
- Focused project-orchestrator and packaged-runtime source suite: 46 tests,
  `OK`.
- Packaged release discovery: 7 tests, `OK`, including all three methodology
  profiles crossed with both roadmap providers.
- The packaged matrix resolves and diagnoses `core`, `lean-matt`, and
  `strict-superpowers` with `none` and `gsd` in isolated repositories. Core
  readiness remains independent; selected external providers fail closed when
  their isolated sources are absent.
- Strict OpenSpec validation, workflow validation, Python/JSON compilation, and
  `git diff --check` are final pre-commit gates.

## Release Promotion and Runtime

- The user authorized continuing repository release, commit, and push after
  reviewing the outstanding status.
- Release promotion synchronized `assets/templates/AGENTS.md.template` and
  `tests/test_packaged_runtime.py`, rebuilt the runtime archive, and removed the
  stale generated bytecode file.
- The required second dry-run reports `status: current`, with no changed,
  missing, deleted, stale, or unmanaged outputs.
- Runtime verification was then executed with `PYTHONDONTWRITEBYTECODE`
  explicitly removed from the environment; it did not create `__pycache__`,
  and the following release dry-run remained `current`.
- Runtime verification reports `ok: true`, `status: verified`, 275 checks, and
  zero failures.
- Runtime archive SHA-256:
  `1aa795c12fb166e753dc8523d2ab56b0a2f79ecef553f06abb2afdedcb6b8a9a`.
- `SOURCE_COMMIT` records the pre-commit source revision by design. Archive and
  manifest source hashes are the release truth gate and avoid a self-referential
  final-commit requirement.

## Plugin Eval

Evaluated target: `plugins/dev-flow` (the release-preferred target).

```text
Score: 86/100
Grade: B
Risk: medium
Checks: 0 fail, 3 warn, 2 info
trigger_cost_tokens: 385
invoke_cost_tokens: 11101
deferred_cost_tokens: 26300
```

The warning IDs remain the established static-budget baseline:

- `trigger_cost_tokens-budget-high`
- `invoke_cost_tokens-budget-high`
- `deferred_cost_tokens-budget-high`

There is no new warning ID and no failure. The residual risk is that static
token cost remains heavy; observed usage is intentionally left to the later
authorized provider outcome study rather than blocking this optional-provider
architecture release.

## Provider Benchmark Disposition

- The dry-run validates a randomized 60-run plan: ten tasks, two profiles, and
  three repetitions.
- `externalModelRunsPerformed: false`, `writesPerformed: false`, and validation
  reports no errors.
- The live/paid comparison and blind human review are recorded as
  `skipped_with_reason` for this change. They are prerequisites for any later
  proposal to replace `core` as the default, not prerequisites for shipping the
  provider seam.
- `core + none` remains the default. `lean-matt`, `strict-superpowers`, and GSD
  remain opt-in.

## Local Reference Dry Run

- Active profile/home: `openai-official`, `/Users/cY/.codex`.
- Configured App CLI remains
  `/Applications/Codex.app/Contents/Resources/codex`; the PATH shim reports its
  version unavailable in this diagnostic.
- The updater ran in dry-run mode and reports
  `dev-flow@cy-codex-skills: would-refresh` plus installed cache
  `differs-from-source`.
- Project migration remains blocked by legacy Superpowers source identity,
  missing explicit provider config, and the active phase/change.
- No plugin cache, provider link, project configuration, migration, or legacy
  skill cleanup was applied.

## Residual Actions

1. Refresh the installed DevFlow cache before relying on this behavior locally.
2. Resolve provider selection and active-change migration conflicts before an
   explicitly authorized real-project migration.
3. Run the live outcome benchmark only as part of a later default-switch
   proposal with separate spend and blind-review authorization.
4. Archive the OpenSpec change only after a separate archive request and a
   clear namespaced-state migration gate.
