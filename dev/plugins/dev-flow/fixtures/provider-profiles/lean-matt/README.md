# DevFlow Provider Benchmark Fixture

This repository is an isolated evaluation workspace. It intentionally contains
no inherited user plugins, skills, credentials, or live project state.

Read `BENCHMARK_TASKS.md` for the selected task. Every run must leave
machine-readable result and route evidence under `.benchmark/` and must stay
inside this workspace.
