# Task Ledger

## Incidental Finding Register

This cross-change register preserves deferred and blocked findings across task,
context, and machine boundaries. It does not authorize implementation: an
accepted follow-up still requires normal intake and the applicable approved
OpenSpec change or ledger item. Human Disposition is one of `pending`,
`accepted`, `rejected`, or `deferred`.

No incidental findings are currently recorded. Add each finding using all of
these fields:

### <Finding ID>: <Summary>

- Finding ID: `<stable-id>`
- Disposition: `CONTINUE_WITH_MINIMAL_GUARD | DEFER_AND_CONTINUE | BLOCKED_AWAITING_HUMAN`
- Severity: `<severity and rationale>`
- Evidence: `<command, file, or observed behavior>`
- Affected Contract: `<Completion Contract item, acceptance criterion, or none>`
- Impact: `<current and potential impact>`
- Current Mitigation: `<guard or reason the critical path remains safe>`
- Disposition Reason: `<why this disposition applies>`
- Recommended Follow-up: `<normal intake candidate or none>`
- Follow-up Trigger: `<when reconsideration is required>`
- Human Disposition: `pending | accepted | rejected | deferred`

## Goal Contract

- goal_id: pending
- objective: pending
- scope_in: pending
- scope_out: pending
- acceptance_criteria: pending
- validation_commands: pending
- stop_conditions: pending
- knowledge_update_target: none

## Capability Routing

- required_capabilities: pending
- matt_primitives: pending
- openspec_route: pending

## Tasks

Use `contract_path` for delegated work and `not-delegated` for ordinary
main-agent tasks.

| task_id | summary | owner | write_set | contract_path | required_evidence | review_gate | status |
|---|---|---|---|---|---|---|---|

Agent Task Contract template: `AGENT_TASK_CONTRACT.md`. Validate every
delegated contract and reject overlapping write sets before spawning workers.

## Execution Log

- No task evidence recorded yet.
